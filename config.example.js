// Copy to config.js and fill in your Supabase project values.
// Local dev (supabase start): run `supabase status` for URL and anon key.
window.MORPHIX_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
  // Optional: override redirect base (defaults to window.location.origin)
  // authRedirectBase: 'https://yiyuanlee.github.io/morphix',
};
